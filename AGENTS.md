# AGENTS.md — 開発 Agent の行動規約

このリポジトリで作業するすべての AI Agent（Codex / Claude Code / その他）は、**作業開始前に本ファイルと `docs/context-summary.md` を読むこと**。本ファイルが他の指示と矛盾する場合、本ファイル（特に「禁止事項」）が優先する。

---

## 0. 一言で言うと

> 自分専用のリアルタイム英→日 音声翻訳 Web アプリ。**秘密情報を漏らさず、翻訳本文を保存・ログ出力せず、許可メールのみ利用可**にする。迷ったら安全側に倒し、勝手に方針を変えず、チケット範囲だけ実装する。

---

## 1. 絶対禁止事項（NON-NEGOTIABLE / 違反したら即停止して人間に確認）

1. 標準 `OPENAI_API_KEY` をフロントエンド（クライアントコンポーネント / ブラウザ送信）に出さない。
2. 秘密情報を `NEXT_PUBLIC_` 付き環境変数に入れない。
3. ブラウザに返してよいのは**短命の client secret（`ek_...`）のみ**。
4. client secret 発行 API は**認証済みかつ許可メール**のユーザーだけが呼べる。それ以外は 401/403。
5. 音声・文字起こし（英語 transcript）・翻訳本文（日本語）・会話内容を **DB に保存しない**。
6. 上記本文を**アプリケーションログ / `console.log` / エラーメッセージ / 例外 stack に出さない**。
7. 上記本文を **localStorage / sessionStorage / IndexedDB / cookie に保存しない**。
8. 実 API キー・実会話ログ・本番秘密情報・実ユーザーデータを**プロンプトやコミットに含めない**。テストは架空かつ無害な短文のみ。
9. MVP では**翻訳履歴機能・DB・外部分析 / トラッキング / エラー監視 SaaS を入れない**。
10. 依存関係を不必要に増やさない（新規 npm パッケージ追加は理由を PR / コミットに明記）。
11. セキュリティ・データ保持について**断定しすぎない**。「一切保持されない」等の過剰表現を docs / UI に書かない。API 事業者側の保持可能性・ZDR の条件は `docs/privacy-data-handling.md` の表現に従う。
12. **チケットの範囲外を勝手に実装しない**。アーキテクチャ判断（認証方式・デプロイ先・モデル・データフローの変更）を独断で変えない。変える必要があれば ADR 案を出して**人間に確認**する。

---

## 2. やること（実装手順の標準フロー）

各チケットは次の順で進める:

1. `docs/context-summary.md` と該当チケット（`TASKS.md`）の受け入れ条件を読む。
2. 影響範囲を 3〜10 行で要約し、変更ファイルを列挙する（実装前の宣言）。
3. チケット範囲のみ実装する。
4. 受け入れ条件を満たすテストを書く / 実行する（本文ログが出ていないことの確認を含む）。
5. `docs/security.md` のチェックリストの**該当項目**を自己確認する。
6. `TASKS.md` のステータスと、必要なら `docs/context-summary.md` を更新する。
7. コミットメッセージに「何を・なぜ」を書く。秘密情報や本文を含めない。

---

## 3. セキュリティ方針（要約）

- 認証: Auth.js (NextAuth v5) + OAuth（既定 Google）。`/translate` と全 API は認証必須。
- 認可: `ALLOWED_EMAILS` の allowlist。一致しなければ 403 相当でセッションを張らない / 拒否。
- client secret: サーバー API route が `OPENAI_API_KEY` を使い `POST /v1/realtime/client_secrets` で短命トークン（既定 TTL 120秒）を発行。発行前に認証 + allowlist を必ずチェック。
- Safety Identifier: メール平文ではなく、`SAFETY_ID_SALT` でハッシュ化した安定 ID を `OpenAI-Safety-Identifier` ヘッダ（サーバー側リクエスト）に付与。
- レート制限: client secret 発行 API にユーザー単位の簡易レート制限を入れる（メモリ / エッジ KV）。
- CSP / CSRF / CORS / cookie: `docs/security.md` の設定に従う（HttpOnly, Secure, SameSite=Lax, 本番 HTTPS 前提）。
- secrets: `.env.local`（コミット禁止）+ デプロイ先の暗号化環境変数。`.env.example` には実値を書かない。

詳細と確認チェックリストは [`docs/security.md`](./docs/security.md)。

---

## 4. ログ禁止方針（要約）

- ログレベルに関わらず、音声・transcript・翻訳本文・会話内容を出力しない。
- 出してよいのは: イベント種別、タイムスタンプ、HTTP ステータス、エラーコード（本文を含まないもの）、匿名化済みユーザー識別子（ハッシュ）。
- WebRTC のデータチャネルで受け取る `delta`（部分翻訳テキスト）を `console.log` しない。デバッグ時も**長さや件数**のみログ可。
- エラーハンドリングで例外オブジェクトをそのまま log しない（本文が混入し得る）。種別とコードに正規化する。

---

## 5. トークンコスト節約方針

- 毎回のコンテキストには **`docs/context-summary.md` + 対象チケット + 触る範囲の docs だけ**を渡す。全履歴・全 docs を渡さない。
- 高思考モデルは設計・分解・レビュー方針・ADR 判断に限定。実装は軽量〜中程度モデルで行う（[`docs/agent-strategy.md`](./docs/agent-strategy.md)）。
- 1 回の Agent 実行は **1 チケット相当**まで。複数 Gate をまたがない。
- 出力は差分中心。無関係ファイルを触らない / 大規模リファクタしない。
- 仕様で迷ったら、推測で広げず最小実装にして「未決事項」として残す。

---

## 6. 確認ゲート（人間承認が必要なポイント）

次は Agent 単独で進めず、人間の承認を得る:

- 認証方式・OAuth プロバイダ・デプロイ先・使用モデルの変更
- データフロー / 保存方針 / ログ方針に影響する変更
- 新規依存の追加、外部 SaaS の導入
- `docs/privacy-data-handling.md` の保持・残余リスク表現の変更
- Gate 通過判定（`docs/agent-strategy.md` の Gate 0〜7）

---

## 7. 完了の定義（Definition of Done / 全チケット共通）

- 受け入れ条件を満たす。
- 本文ログ・本文保存・秘密情報露出が無いことを確認済み。
- 該当するセキュリティチェック項目を確認済み。
- `TASKS.md` 更新済み。lint / typecheck / テストが通る。
- チケット範囲外の変更を含まない。
